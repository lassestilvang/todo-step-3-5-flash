# Database directory path (absolute or relative to project root)
# Defaults to ./data if not set
# DATABASE_PATH=./data

# Site URL for sitemap and robots.txt (required for production)
# NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# Node environment (set by system, usually not needed)
# NODE_ENV=production
